# Optional: URL of the deployed RAS/OS storage worker (worker/).
# When set, user files sync to the Cloudflare Worker (source of truth) with a
# localStorage write-through cache. When unset, files live only in localStorage.
VITE_STORAGE_URL=
